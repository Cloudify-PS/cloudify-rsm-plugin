inputs:
  resources:
    value:
      resources:
        openstack:
          nova:
            memory: 1
            cores: 1
            storage: 2
        aci:
         links: 20
      quota:
        openstack:
          nova:
            memory: 10
            cores: 10
            storage: 80
         aci:
           links: 20


  check_profile:
    value:  small_profile

  profiles:
    value:
      small_profile:
        openstack:
          nova:
            memory: 4
            cores: 2
            storage: 20
        aci:
          links: 20

      big_profile:
        openstack:
          nova:
            memory: 8
            cores: 4
            storage: 40
        aci:
          links: 20

node_template:

  openstack_projectA:
    type:


  openstack_global:
    type:
      interfaces:
        cloudify.interfaces.lifecycle.create:


plugins:



workflows:


  resource-profile-validate:
    mapping: resources.cloudify_resources.tasks.validate_profiles_workflow
    parameters:
      profiles:
        default: []
      operations:
       default:
          - list

      global_nodes:
       default:
          - openstack_resource_pool
          - n7k_resource_pool
          - aci_resource_pool
      project_nodes:
       default:
          - openstack_resource_pool
          - n7k_resource_pool
          - aci_resource_pool
